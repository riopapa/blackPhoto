<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        ...
        <item name="android:actionButtonStyle">@style/myActionButtonStyle</item>
        <item name="actionButtonStyle">@style/myActionButtonStyle</item>
        ...
    </style>
    <style name="myActionButtonStyle" parent="Widget.AppCompat.ActionButton">
        <item name="android:minWidth">30dp</item>
        <item name="android:maxWidth">48dp</item>
        <item name="android:width">38dp</item>
    </style>

    <style name="scrollbar_shape_style">
        <item name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <item name="android:scrollbarStyle">outsideOverlay</item>
        <item name="android:scrollbars">vertical</item>
        <item name="android:fadeScrollbars">true</item>
        <item name="android:scrollbarThumbVertical">@drawable/scrollbar_vertical_thumb</item>
        <item name="android:scrollbarTrackVertical">@drawable/scrollbar_vertical_track</item>
        <item name="android:scrollbarSize">4dp</item>
        <item name="android:scrollbarFadeDuration">2000</item>
        <item name="android:scrollbarDefaultDelayBeforeFade">1000</item>
    </style>

    <style name="MyPreference" parent="Theme.AppCompat">
        <item name="title">설정</item>
        <item name="titleTextColor">#1EC2F4</item>  // title bar text color
        <item name="colorPrimary">#77A5D3</item>    // title bar background color
        <item name="android:colorBackground">#77A5D3</item> // setting screen background

        <item name="colorAccent">#FFFF00</item> // category, switch button color

        <item name="android:textColor">#045E04</item>   // key title color
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item>
        <item name="colorSecondary">#E4C5C4</item>    //
        <item name="colorPrimarySurface">#5C0606</item>
        // summary color variable not known
    </style>
</resources>
